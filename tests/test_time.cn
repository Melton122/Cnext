import time

main {
    int now = time_now()
    printin(now > 0)

    int ms = time_now_ms()
    printin(ms > 0)

    int y = time_year(now)
    printin(y >= 2020)

    int m = time_month(now)
    printin(m >= 1 && m <= 12)

    int d = time_day(now)
    printin(d >= 1 && d <= 31)
}
