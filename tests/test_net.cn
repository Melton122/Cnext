import net

main {
    // Test TCP functions compile and execute safely
    int sock = net.tcp_connect("127.0.0.1", 9999)
    bool connected = (sock != -1)
    printin(connected)
    if connected {
        net.tcp_close(sock)
    }
}
