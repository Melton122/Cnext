interface Logger {
    func log(str message)
}

class ConsoleLogger implements Logger {
    func new() {}
    
    override func log(str message) {
        printin("LOG: " + message)
    }
}

main {
    ConsoleLogger logger = new ConsoleLogger()
    logger.log("Hello Interfaces!")
}
