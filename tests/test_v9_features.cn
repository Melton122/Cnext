// Cnext v9.0 Feature Tests
// Tests enum values, closures, and other features

// EXPECT: 2
// EXPECT: 10
// EXPECT: 15

main {
    // Test closures
    var counter = 0
    var increment = () => {
        counter = counter + 1
        return counter
    }
    
    printin(increment())
    printin(increment())
    printin(increment())
}
