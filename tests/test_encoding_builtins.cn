// EXPECT: aGVsbG8=
// EXPECT: hello
// EXPECT: true
main {
    // base64 encode
    str b64 = base64_encode("hello")
    printin(b64)

    // base64 decode round-trip
    str raw = base64_decode(b64)
    printin(raw)

    // verify round-trip
    printin(raw == "hello")
}
